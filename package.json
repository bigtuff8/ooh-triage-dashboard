{
  "name": "ooh-dashboard",
  "version": "1.0.2",
  "description": "OOH Dashboard — guided triage and device control for out-of-hours call handlers (SD-492 write path, B2C SSO, Zendesk ticketing)",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed"
  },
  "dependencies": {
    "@azure/cosmos": "^4.9.3",
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^8.3.0",
    "express-session": "^1.19.0",
    "openid-client": "^6.8.4"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
