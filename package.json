{
  "name": "ooh-triage-dashboard",
  "version": "0.2.0",
  "description": "Out of Hours Triage Dashboard — guided flows for OOH call handlers to triage and resolve IoT site issues",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed"
  },
  "dependencies": {
    "amazon-cognito-identity-js": "^6.3.16",
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^8.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
